import { PATH_DASHBOARD } from '../../routes/paths';
import { faImagePortrait } from '@fortawesome/free-solid-svg-icons';

export const LinkItems = [
  { name: 'Photos', icon: faImagePortrait, link: PATH_DASHBOARD.general.app },
];
